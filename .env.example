# Copiar a .env (no versionado) y ajustar si hace falta.
# .env ya está en .gitignore — nunca subir credenciales reales al repo.

POSTGRES_DB=olimpiadas
POSTGRES_USER=olimpiadas_app
POSTGRES_PASSWORD=cambiar_esta_clave
POSTGRES_PORT=5434
